Run this:

```
wpctl status
```

You should see a "Audio" section and on it a "Sink" subsection. The item with a "*" is the default.

Use the following command to define a item as default:

```
wpctl set-default ITEM_NUMBER
```
